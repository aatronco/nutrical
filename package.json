{
  "name": "brute",
  "version": "1.0.0",
  "description": "Brute — GZCLP v6 workout + nutrition PWA",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18" }
}
